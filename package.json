{
  "name": "dyslexer",
  "version": "0.4.3",
  "description": "Parser",
  "main": "lib/dyslexer.js",
  "scripts": {
    "test": "mocha --opts config/mocha.opts",
    "watch": "npm -s test -- --watch",
    "lint": "standard src/**/*.js test/**/*.js *.js",
    "format": "standard --format src/**/*.js test/**/*.js *.js",
    "preinstall": "npm -s build",
    "prepublish": "npm -s build",
    "build": "babel src --out-dir lib"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/rhalff/dyslexer.git"
  },
  "author": "Rob Halff",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/rhalff/dyslexer/issues"
  },
  "standard": {
    "parser": "babel-eslint"
  },
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-core": "^6.5.2",
    "babel-eslint": "^4.1.8",
    "babel-preset-es2015": "^6.5.0",
    "babel-preset-stage-0": "^6.5.0",
    "chai": "^3.5.0",
    "mocha": "2.x.x",
    "sinon": "^1.17.3",
    "sinon-chai": "^2.8.0",
    "standard-format": "^2.1.0"
  },
  "dependencies": {
    "standard": "^6.0.5"
  }
}
